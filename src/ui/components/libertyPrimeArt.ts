/**
 * The art for the warning before a collection install: Liberty Prime, who
 * walks through Fallout lasering anything that gets in the way.
 *
 * A data URI for the same reason as the Event Horizon mark
 * (eventHorizonMarkData.ts): the UI renders inside Vortex's window, where a
 * file path can silently fail to resolve. Undefined until the painted image
 * exists; until then the warning shows its quote without a picture.
 */
export const LIBERTY_PRIME_ART: string | undefined = undefined;
